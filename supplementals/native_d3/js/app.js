// chart parameters
var chartWidth = 1000;
var chartHeight = 800;
var padding = 50;

// svg
var svg = d3.select('#chart')
    .append('svg')
    .attr('width', chartWidth)
    .attr('height', chartHeight);

